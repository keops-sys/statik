version: '2'
services:
  server:
    image: pagespeed/nginx-pagespeed
    container_name: statik
    restart: always
    networks:
      - web
    labels:
      # - traefik.http.routers.NOM-SERVICE-http.entrypoints=http
      # - traefik.http.routers.NOM-SERVICE-http.rule=Host(`statik.keops.io`)
      
      # - traefik.http.middlewares.https-redirect.redirectscheme.scheme=https
      # - traefik.http.middlewares.https-redirect.redirectscheme.permanent=true
      # - traefik.http.routers.NOM-SERVICE-http.middlewares=https-redirect@docker
      
      - traefik.http.routers.NOM-SERVICE-https.entrypoints=https
      - traefik.http.routers.NOM-SERVICE-https.rule=Host(`statik.keops.io`)
      - traefik.http.routers.NOM-SERVICE-https.tls=true
      - traefik.http.routers.NOM-SERVICE-https.tls.certresolver=letsencrypt
    volumes:
      - ./public:/usr/share/nginx/html
    environment:
      - NODE_ENV=production
      - url=statik.keops.io

networks:
  web:
    external: true